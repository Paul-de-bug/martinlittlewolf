# 2027 solution images

Place the 2027 solution images in this folder.

- Name the splash image `0.svg` (or `0.png`).
- Name the puzzle solutions `1.svg` through `224.svg` (or use the same numbers with `.png`).
- If both formats exist for a number, the website uses the SVG.
